# Source snapshot note

This directory is a dead, text/source-code snapshot cloned from `stpierrs/conceptual_flat_earth_model` for local analysis inside `joegibes/experiments`.

Do not push to or otherwise interact with the original upstream repository from here. Binary image assets from the upstream clone were intentionally omitted from this committed snapshot so the audit PR stays text/SVG-only.
